import gin.torch.external_configurables

# Parameters for GruModel:
# ==============================================================================
GruModel.config = \
    {'dropout': 0.2,
     'hidden_size': 64,
     'input_size': 1,
     'num_layers': 2,
     'output_size': 1}
