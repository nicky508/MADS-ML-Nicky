import gin.torch.external_configurables

# Parameters for EegModel:
# ==============================================================================
EegModel.config = \
    {'dropout': 0.2,
     'hidden_size': 64,
     'input_size': 14,
     'num_layers': 2,
     'output_size': 1}
