import gin.torch.external_configurables

# Parameters for GruModel:
# ==============================================================================
GruModel.config = \
    {'dropout': 0.1,
     'hidden_size': 128,
     'input_size': 128,
     'num_layers': 8,
     'output_size': 20}
