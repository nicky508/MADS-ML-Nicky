import gin.torch.external_configurables

# Parameters for GruModel:
# ==============================================================================
GruModel.config = \
    {'dropout': 0.1,
     'hidden_size': 64,
     'input_size': 64,
     'num_layers': 8,
     'output_size': 20}
